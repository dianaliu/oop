LEAF TRANSLATOR - TRIVIAL (kirim?)
- Add support for multiple ASTs.
  for each AST in AST[], translate & output
- Produce CPP code that compiles and runs :
  #include, main returns type int, re-order structs and typedefs
- Support variable arguments for System.out.println
- Translate all expressions :
  for, do/while, switch, etc.

LEAF TRANSLATOR - TEDIOUS (rob & diana?)
- Translate method invocations using vtable
- Translate constructors using vtable and data layout
- Fix Method Header : 
  get modifiers, class name, parameters, return type from vtable
- Produce CPP code that compiles and runs

TESTING (hernel?)
- Automate running test Java and CPP files and comparing output
- Rehearse presentation
  10 minutes : Presentation & Demo
  5 minutes  : Questions

EXTRAS (lol)
- Translate arrays
